/**
 * The X/Y/Z axis colors (red/green/blue - the usual gizmo convention), shared so the overlays that
 * mark an axis - the modal transform guides, the ground grid's lines through the origin - cannot
 * drift apart.
 */

export const AXIS_COLORS = [0xff5566, 0x55dd66, 0x5599ff] as const;
